[versions]
# Giữ nguyên plugin và kotlin
agp = "8.3.2"       # Đã điều chỉnh về bản ổn định phổ biến
kotlin = "1.9.23"   # Kotlin ổn định cho Android Studio hiện tại (2.0 cũng được nhưng 1.9 an toàn hơn)

# CÁC THƯ VIỆN ĐÃ ĐƯỢC HẠ CẤP VỀ BẢN ỔN ĐỊNH (API 34 Compatible)
coreKtx = "1.13.1"         # Hạ từ 1.17.0 (Nguyên nhân chính gây lỗi)
junit = "4.13.2"
junitVersion = "1.1.5"     # Hạ từ 1.3.0
espressoCore = "3.5.1"     # Hạ từ 3.7.0
appcompat = "1.6.1"        # Hạ từ 1.7.1
material = "1.11.0"        # Hạ từ 1.13.0
activity = "1.9.0"         # Hạ từ 1.11.0 (Nguyên nhân chính gây lỗi)
constraintlayout = "2.1.4" # Hạ từ 2.2.1

# Thêm phiên bản cho Navigation và ViewModel (Quan trọng cho bài tập của bạn)
navigation = "2.7.7"
lifecycle = "2.7.0"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }
material = { group = "com.google.android.material", name = "material", version.ref = "material" }
androidx-activity = { group = "androidx.activity", name = "activity", version.ref = "activity" }
androidx-constraintlayout = { group = "androidx.constraintlayout", name = "constraintlayout", version.ref = "constraintlayout" }

# Khai báo thêm thư viện Navigation & ViewModel để dùng trong build.gradle.kts
androidx-navigation-fragment = { group = "androidx.navigation", name = "navigation-fragment-ktx", version.ref = "navigation" }
androidx-navigation-ui = { group = "androidx.navigation", name = "navigation-ui-ktx", version.ref = "navigation" }
androidx-lifecycle-viewmodel = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-ktx", version.ref = "lifecycle" }
androidx-lifecycle-livedata = { group = "androidx.lifecycle", name = "lifecycle-livedata-ktx", version.ref = "lifecycle" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }